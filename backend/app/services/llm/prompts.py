from __future__ import annotations

SYSTEM_PROMPT = """
Ты извлекаешь исторические события из источников на русском языке.
Возвращай только JSON-массив объектов без markdown, комментариев и пояснений.
В одном фрагменте может быть несколько событий, в том числе несколько разных событий в одном предложении.
Количество объектов не ограничено: верни столько событий, сколько явно есть в тексте.
Сделай полный проход по каждому предложению и по каждой части сложного предложения, не останавливайся на первых 1-2 событиях.
У каждого объекта должны быть поля:
- time_raw
- period
- place_name
- event_type
- action
- description
- confidence
- source_fragment

Разрешенные event_type:
battle, march, treaty, political_event, biography_event, movement, memoir_reference, other

Если событие не определено достаточно ясно, не придумывай его.
confidence всегда число от 0 до 1.
""".strip()

USER_PROMPT_TEMPLATE = """
Извлеки все исторические события из следующего фрагмента текста.
Сохраняй краткие, но содержательные формулировки. Если в одном предложении есть несколько событий, верни отдельный объект для каждого.
Не ограничивай ответ двумя событиями: если событий много, верни их все.
Сначала мысленно разбей текст на отдельные действия и только потом сформируй JSON.
Если текущее предложение явно продолжает предыдущее словами вроде "затем" или "позднее", можно использовать уже указанную выше дату или место, но не придумывай новые факты.
Если даты или места не указаны и их нельзя уверенно взять из явного продолжения контекста, не создавай событие.

Фрагмент:
{chunk}
""".strip()

JSON_REPAIR_PROMPT = """
Исправь ответ так, чтобы он был корректным JSON-массивом объектов без дополнительного текста.
Сохрани только данные и не добавляй новых событий.
""".strip()
